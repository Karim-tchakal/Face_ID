# FaceID — Python Dependencies
# ===========================
# Core vision
opencv-python>=4.8.0
mediapipe>=0.10.0
numpy>=1.24.0

# ArcFace / deep face embeddings
insightface>=0.7.3
onnxruntime>=1.16.0          # Use onnxruntime-gpu for GPU acceleration

# UI Support
Pillow>=10.0.0

# Standard Library (for reference, no need to pip install):
#   - os, json, time, queue, threading, urllib.request, datetime, pathlib, shutil, tkinter, subprocess

# Raspberry Pi 5 notes:
#   - Ensure python3-tk is installed via apt:
#     sudo apt install python3-tk
#   - Ensure libgomp1 is installed via apt for InsightFace:
#     sudo apt install libgomp1
