# FaceID Application — Python Dependencies
# ==========================================
# Install on Raspberry Pi 5 or Linux/Windows dev machine
#
# Quick install:
#   pip install -r requirements.txt
#
# For GPU (CUDA) support with ArcFace:
#   pip install onnxruntime-gpu   (instead of onnxruntime)

# Core vision
opencv-python>=4.8.0
mediapipe>=0.10.0
Pillow>=10.0.0
numpy>=1.24.0

# ArcFace / deep face embeddings
insightface>=0.7.3
onnxruntime>=1.16.0          # Use onnxruntime-gpu for GPU acceleration

# Raspberry Pi 5 notes:
#   - mediapipe wheels for ARM64 may need to be built from source
#     or installed via: pip install mediapipe-rpi4 / mediapipe-rpi5
#   - insightface on ARM64:
#       sudo apt install libgomp1
#       pip install insightface --extra-index-url https://download.pytorch.org/whl/cpu
#   - For hardware camera (CSI):
#       Use /dev/video0 — libcamera-vid pipeline or v4l2 wrapper

# Tkinter is part of Python stdlib — ensure it's installed:
#   sudo apt install python3-tk